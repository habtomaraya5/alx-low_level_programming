#include "main.h"
/**
 * _abs - function for absolut numbers for all
 * Description: absolute checker at all
 *@x: is used for input parameter for those
 * Return:0
 */
int _abs(int x)
{
if (x == 0 || x > 0)
return (x);
else
return (x * -1);
}
